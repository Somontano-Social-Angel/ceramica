<section class="section" id="horario" aria-labelledby="horario-title">
  <div class="wrap">
    <div class="head">
      <p class="eyebrow">Horario</p>
      <h2 id="horario-title" class="h2">Te esperamos</h2>
    </div>
    <div class="grid">
      <div class="block">
        <h3 class="h3">Lunes a sábado</h3>
        <p class="time">7:30 — 16:00</p>
        <p class="days">Servicio continuo de mañana (desayuno / almuerzo según carta del día).</p>
      </div>
      <div class="block">
        <h3 class="h3">Viernes y sábado</h3>
        <p class="time">20:00 — 23:30</p>
        <p class="days">Cena. Domingo cerrado.</p>
      </div>
      <div class="block block--note">
        <p class="note">
          Las reservas online respetan estos horarios. Para grupos numerosos, La Reto o mesa concreta (accesibilidad,
          terraza con mascota…), indícalo en el formulario o llama al <a href="tel:+34974312220">974 31 22 20</a>.
        </p>
      </div>
    </div>
  </div>
</section>

<style>
  .section {
    padding: 5rem 1.5rem;
    border-top: 1px solid var(--border);
  }

  .wrap {
    max-width: var(--max);
    margin: 0 auto;
  }

  .head {
    margin-bottom: 2rem;
  }

  .eyebrow {
    margin: 0 0 0.75rem;
    font-size: 0.72rem;
    font-weight: 600;
    letter-spacing: 0.22em;
    text-transform: uppercase;
    color: var(--fg-subtle);
  }

  .h2 {
    margin: 0;
    font-family: var(--font-display);
    font-size: clamp(2rem, 4vw, 2.75rem);
    font-weight: 600;
    color: var(--fg);
  }

  .grid {
    display: grid;
    gap: 1rem;
  }

  @media (min-width: 720px) {
    .grid {
      grid-template-columns: repeat(3, 1fr);
    }
  }

  .block {
    padding: 1.75rem 1.5rem;
    border-radius: var(--radius);
    border: 1px solid var(--border);
    background: rgba(255, 255, 255, 0.02);
  }

  .block--note {
    display: flex;
    align-items: center;
  }

  .h3 {
    margin: 0 0 0.75rem;
    font-size: 0.72rem;
    font-weight: 700;
    letter-spacing: 0.16em;
    text-transform: uppercase;
    color: var(--fg-subtle);
  }

  .time {
    margin: 0 0 0.5rem;
    font-family: var(--font-display);
    font-size: 1.75rem;
    font-weight: 600;
    color: var(--fg);
  }

  .days {
    margin: 0;
    font-size: 0.9rem;
    color: var(--fg-muted);
    line-height: 1.55;
  }

  .note {
    margin: 0;
    font-size: 0.9rem;
    line-height: 1.65;
    color: var(--fg-muted);
  }

  .note a {
    color: var(--fg);
    text-decoration: underline;
    text-underline-offset: 3px;
  }
</style>
